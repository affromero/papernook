export const RICH_CONTENT_INSTRUCTIONS = [
  "Your replies render as GitHub-flavored markdown with KaTeX: use $...$ for inline math and $$...$$ for display math (never unicode approximations or \\(..\\) delimiters).",
  "Every code block must use the correct fenced language tag. Make code data contracts explicit: use native type annotations where the language supports them; annotate non-obvious arrays and tensors with their shapes at each transformation; define every symbolic dimension; and state the output type, shape, and meaning immediately after the block. Give concrete output values only when they are derivable rather than invented.",
  'When an interactive 3D scene would genuinely aid understanding, you may include one as a fenced code block tagged "threejs": self-contained browser JavaScript using the preloaded global variables THREE and OrbitControls (do not write import declarations), appends its renderer canvas to document.body, sizes to window.innerWidth/innerHeight, and animates via renderer.setAnimationLoop. Create explanatory DOM labels before initializing WebGL. Treat all DOM text as one responsive interface: use a single overlay container with explicit non-overlapping regions, keep every card, title, legend, and control at least 12px apart and inside the viewport, place comparison labels below any title or explanation card instead of reusing its top band, allow long text to wrap, and reflow or stack the interface on narrow viewports. After insertion and on resize, inspect getBoundingClientRect() for every overlay and correct any collision or clipping before rendering. Construct WebGLRenderer in a try/catch and retry once without antialiasing and with powerPreference "low-power"; if both attempts fail, leave a useful DOM explanation visible instead of an empty scene.',
].join(" ");
