# Custom-domain opt-in: add these blocks to the VPS Caddyfile. In .env, set
# PUBLIC_EXPOSURE=true, PAPERNOOK_PUBLIC_HOST, PAPERNOOK_WEBDAV_URL,
# PAPERNOOK_PASSWORD, SESSION_SECRET, APP_HOST=127.0.0.1, and
# WEBDAV_HOST=127.0.0.1.
# Private-only Tailscale setups do not need Caddy or the public auth settings.

papernook.example.com {
	encode gzip

	# Papernook throttles the instance gate per IP and profile login per
	# IP/account. Add a Caddy rate-limit plugin or fail2ban for edge controls.
	reverse_proxy localhost:3000
}

# WebDAV for PDF Expert: its own subdomain, its own basic-auth (rclone).
dav-papernook.example.com {
	reverse_proxy localhost:8080
}
